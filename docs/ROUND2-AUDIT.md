# Round-2 audit & cleanup (pre-GitHub push)

> Verified against the actual repo at 2026-05-18.

## Repo state

- **Not a git repo yet.** `git status` reports `fatal: not a git repository`. First push = `git init` then a single `main` branch — no stale branches to delete.
- Lockfile + workspace files present (`pnpm-lock.yaml`, `pnpm-workspace.yaml`).
- 28 test files passing; web bundle builds.

## Findings

### A. Files in the wrong place

| File | Issue | Action |
|---|---|---|
| `keres_ai_email_automation.html` (252 KB) | Original design prototype; already ported into the React SPA. User confirmed it's no longer necessary. | Move to `docs/_design/keres_ai_email_automation.html` for historical reference. |
| `ARCHITECTURE-V3-ECONOMICS.md` (56 KB) | Original input doc; superseded by `docs/UPDATED-ARCHITECTURE.md` which already cites it. | Move to `docs/_design/`. |
| `ARCHITECTURE-V3.1-SUB-5.md` (25 KB) | Same. | Move to `docs/_design/`. |
| `VALIDATION-PLAN.md` (32 KB) | Original input doc; `docs/VALIDATION-MODE.md` is the operator-facing version. | Move to `docs/_design/`. |
| `.claude/settings.local.json` | Claude Code IDE permission allowlist. Per-machine, not project state. | Add `.claude/` to `.gitignore`; do not commit. |
| `apps/web/dist/` (1.2 MB) | Build artifact. | Already in `.gitignore`. Verified safe. |

### B. .gitignore additions

Current `.gitignore` covers `node_modules/`, `dist/`, `.env*`, `*.log`, `.DS_Store`, `.vite`, `coverage`, `.cache`, `.pnpm-store`, `data/`.

Missing entries for a public-repo push:
- `.claude/` — Claude Code local settings
- `.vscode/` — per-developer VS Code prefs (don't impose mine)
- `.idea/` — JetBrains
- `*.tgz` / `*.tar.gz` — accidental tarball artifacts
- `Thumbs.db` — Windows shell

### C. Tech-debt items I considered and the decision on each

| Item | Verdict | Why |
|---|---|---|
| ESLint not configured (`pnpm lint` is a no-op) | Defer | Documented in `docs/CI.md`; adding it now risks introducing flaky rules in the same commit as the first push. Track as a follow-up. |
| A handful of `as any` casts around Drizzle union types | Keep | Isolated to one file each (`db/client.ts`, a few JSONB columns). Documented inline. |
| `audit.ts` swallows write errors | Keep | Intentional and commented — "never let audit-log writes break the request". |
| `console.log` in `seed.ts` + startup config errors in `index.ts` | Keep | Both are CLI/boot-time stderr output, not request-time noise. |
| `apps/web/dist/index.html` source-map size | Keep | Vite bundles a sourcemap that's only generated by `pnpm build`. Gitignored. |
| `.claude/` directory | Excise | Added to `.gitignore`. |

### D. Sanity sweep — no surprises

- Grepped `TODO|FIXME|XXX|HACK|STUB` across `apps/` and `packages/` → 0 hits.
- Grepped `process.env.AUTH_TOKEN` / `process.env.SES_*` → only in `apps/server/src/config.ts` (the single allowed reader) and in the `apps/server/integration/integration.test.ts` (where it sets test values).
- Grepped for `localStorage|sessionStorage` in `apps/web/src/` → 0 hits. (All persistence is server-side.)
- `.env.example` contains no secrets, only placeholder names + a couple of sample values.
- The HTML mock at root had the CSS selector error we just fixed.

### E. Things that should NOT change in this pass

- The 173 passing tests.
- The launch-gate / compliance-gate behaviour.
- The `validateConfig()` startup checks.
- The provider env flags / cost guards.
- The `.env.example` template (no secrets!).
- The two SAMPLE_MODE/prod branches in the test-server / DNS check (intentional design).

## Cleanup actions executed in this round

1. Move 4 design files into `docs/_design/`.
2. Append `.claude/`, `.vscode/`, `.idea/`, `*.tgz`, `*.tar.gz`, `Thumbs.db` to `.gitignore`.
3. Refresh README to point at `docs/_design/` for the historical inputs.
4. Run `pnpm typecheck && pnpm test && pnpm build` one more time to confirm nothing broke.
5. `git init` → single `main` branch → first commit with a sensible message.

The push target (remote URL) is asked from the operator before any push happens.
