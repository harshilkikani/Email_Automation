# Design inputs (historical)

These files are the original briefs + design prototype that shaped the product. They are kept here for traceability; the *production* documentation lives one level up in `docs/`.

| File | Replaced by |
|---|---|
| `keres_ai_email_automation.html` | The React SPA under `apps/web/`. |
| `ARCHITECTURE-V3-ECONOMICS.md` | `docs/UPDATED-ARCHITECTURE.md` + `docs/COST-MODEL.md` + `docs/SCORING.md`. |
| `ARCHITECTURE-V3.1-SUB-5.md` | Same. |
| `VALIDATION-PLAN.md` | `docs/VALIDATION-MODE.md` (operator playbook) + product `Validation` page. |

Treat anything here as historical context, not as a spec the code should match. If a contradiction exists, the operator-facing docs win.
